* Upcoming

** feat: edit cells in markdown

** feat: export markdown

** feat: store items on the server

** feat: actually log in to something

** fix: sig pipe on the server

** fix: service worker makes the site available offline

~offline~, the initial effort, didn't go great. Messages aren't
connected in there and the ongoing debugging experience in another
window is confusing. Might do just the fetch part as pure JS later,
and leave the network part in a the webworker.

- [ ] add html resources to the caches object
- [ ] add a fetch handler

** feat: command line companion history

Display the local history of the focused cell following the cursor.

** TODO feat: count votes and show the results

- [ ] keyboard shortcut for voting
- [ ] voting interface in #cli
- [ ] display voted results
- [ ] count tabulates the results and sends them
- [ ] display counted results

* Complete

** feat: command line style interface

This changed a bunch in the doing.

I'd like to have global keyboard shortcuts for navigating and editing.
One side benefit: markdown can always appear in the entry box and the
corresponding HTML in the table.

- [X] global keyboard shortcut handler for
  - [X] cursor selection
  - [X] editing
- [X] cursor items, unsaved in the indexdb but broadcast
  - [X] ~Cli.dims~ doesn't update the cursor.edge correctly
- [X] bottom bar for the editor and +shortcut help+
- [X] hideable right side bar for the history
- [X] add to the dimensions by navigating off the table
- [X] scrolling

** fix: handle drift exceptions in hulc timestamp exchange

We've been chatting about the HULC a lot at work, and I never put the
drift and counter exception handling into the timestamp exchange in
here.

** feat: vote

- [X] use DOMplate to make a more realistic ballot box
  - [X] move all the templates off together not in the table
  - [X] clone table rows and cells in draw
- [X] respond to item events to show/hide ballot boxes
  - [X] do the rest of the client side item encoding
  - [X] draw ballot boxes
  - [X] attach events
  - [X] cancel the vote
- [X] color the cells with their vote results
- [ ] attach results when voting is done and use that for the color too
- [X] minimize vote events in the history
- [X] click to edit fills the text without extra space
- [X] highlight the voting button while voting and show vote colors only then

*** counting the vote

Voting works a little differently than I was thinking; in particular
the IDs aren't necessary. I think that voting is a local behavior you can do
anytime, and it's only results that coordinated in rounds. As a
result:

- [X] remove the ID from vote messages
- [X] ignore non-items in a safe way
- [ ] issue ~Result~​s which claim to summarize all ~Vote~​s with
  earlier timestamps
  - [ ] install the result colors iff you're not voting and restore
    them when you stop voting
- [X] remove the ~Call~ item
- [ ] use ~Count~ only to count on the server
- [ ] click votey to enter voting which:
  - [ ] uses the color of your own votes

** feat: keep some or all stale history items and display them

- [X] new section that displays changes over time
- [X] insert events in the correct order
- [X] insert all the events, keeping those removed from table

** fix: client websocket disconnects other websockets for that client

- [X] hashtable by username
- [X] username in the client
- [X] close existing client first

** fix: dream send and recv in the same place to avoid crashing

See https://github.com/camlworks/dream/issues/230 and
https://github.com/camlworks/dream/issues/378

Workaround the crash by doing recv and send in the same place so that
we can always do recv before send, minimizing the crash window.

- [X] add client with a queue for outbound messages
- [X] client runner loop reads and sends when not empty
- [X] world holds the wrapped websockets

After making these changes, it's clear that without read timeouts, the
best I can do is make broadcast only when the client sends. I can do
an LWT timeout to force close and reopen the websocket, but that's a
lot of overhead; timing out without closing the socket causes the next
read to fail. For now, I've elected to send application level pings
from the brower to get the loop to disconnect.

** fix: indexdb needs to store multiple tables

- [X] table id stored and queried with items so we can use one object
  store, since object store can only be created on version changes.
- [X] create an index on table name
- [X] save items with table name
- [X] load items with keyrange query for table name
- [X] use the hulc for the item key
- [X] support deleting items

** feat: edit the title and user

- [X] sign-in sets the user cookie and directs to index
- [ ] index lists tables and allows creating a new one
- [X] title is an editable field which does not change the URL
- [X] read the user from a cookie controlled by the server
